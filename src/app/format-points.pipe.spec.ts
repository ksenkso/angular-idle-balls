import { FormatPointsPipe } from './format-points.pipe';

describe('FormatPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPointsPipe();
    expect(pipe).toBeTruthy();
  });
});
